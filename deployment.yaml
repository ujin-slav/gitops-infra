apiVersion: apps/v1
kind: Deployment
metadata:
  name: react-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: react-app
  template:
    metadata:
      labels:
        app: react-app
    spec:
      containers:
        - name: react-app
          image: ujinslav/gitops01:6439f232b03ceebf90ce65c34efc84cac02cb990
          imagePullPolicy: Always
          ports:
            - containerPort: 80
